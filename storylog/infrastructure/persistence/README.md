# Persistence

This folder is reserved for PostgreSQL-specific persistence code.

Keep database access, SQL, migrations integration, and repository implementations here so the `app/` layer stays focused on Next.js routing and presentation.
